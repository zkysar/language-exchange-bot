app = "discord-host-scheduler-prod"
primary_region = "sjc"

[build]

[env]
  BOT_ENV = "prod"

[processes]
  app = "python -m src.bot"

[[vm]]
  size = "shared-cpu-1x"
  memory = "256mb"

[deploy]
  strategy = "immediate"
